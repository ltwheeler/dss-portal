<h1>Listing favorites</h1>

<table>
  <tr>
    <th>Position</th>
    <th>App_id</th>
    <th>loginid</th>
    <th>is_bookmark</th>
    <ht>name</th>
   <th> </th>
    <th></th>
    <th></th>
  </tr>

<% @favorites.each do |favorite| %>
  <tr>
    <td><%= favorite.position %></td>
    <td><%= favorite.app_id %></td>
    <td><%= favorite.loginid %> </td>
    <td><%= favorite.is_bookmark %> </td>
    <td><%= favorite.name %> </td>
    <td><%= link_to 'Show', favorite %></td>
    <td><%= link_to 'Edit', edit_favorite_path(favorite) %></td>
    <td><%= link_to 'Destroy', favorite, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Favorite', new_favorite_path %>

